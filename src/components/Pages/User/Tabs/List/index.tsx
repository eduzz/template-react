import { Card, Typography } from 'material-ui';
import React, { PureComponent } from 'react';

export default class UserTabList extends PureComponent {
  render() {
    return (
      <Card>
        <Typography>List</Typography>
      </Card>
    );
  }
}