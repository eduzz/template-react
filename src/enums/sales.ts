export enum ProducerType {
  'EMAIL' = 1,
  'NAME' = 2,
  'ID' = 3
}

export enum DateType {
  'CREATED' = 1,
  'CLICK' = 2,
  'CREDIT' = 3
}

export enum InvoiceStatus {
  'OPEN' = 1,
  'PAID' = 2,
  'CANCELED' = 3,
  'DUPLICATED' = 4
}
